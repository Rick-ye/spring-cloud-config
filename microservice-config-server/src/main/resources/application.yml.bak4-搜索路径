server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rick-ye/config-repo  #共用
          search-paths:
          - foo      #路径
          - bar       #路径
          